spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/booklibrary
    username: postgres
    password: 1234
#    schema:
#      - schema.sql
#    data:
#      - data.sql
server:
  servlet:
    context-path: '/book-library'
springdoc:
  swagger-ui:
    path: '/'
# Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: health, info